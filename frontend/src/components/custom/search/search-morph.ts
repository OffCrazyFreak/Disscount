/**
 * Shared between the bar's centre cell and the search sheet's field, so Motion
 * morphs one into the other rather than cross-fading two unrelated surfaces.
 * This is what iOS 26 does with its new search tab role.
 */
export const SEARCH_MORPH_LAYOUT_ID = "search-surface";
