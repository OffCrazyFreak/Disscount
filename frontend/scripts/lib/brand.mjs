// The one definition of the brand green. Every generator reads it from here, so
// a palette change cannot land in one script and be missed in another.
export const GREEN = "#2ec50d"; // --primary
export const GREEN_RGB = [46, 197, 13];
